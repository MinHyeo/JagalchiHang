using System;
using System.Collections;
using System.Runtime.CompilerServices;
using UnityEngine;
using UnityEngine.Windows;

public class TestOpenUICode : MonoBehaviour
{
    //// 플레이어 추가
    //private void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.E))
    //    {
    //        if (UIManager.Instance.IsOpenUI(UIType.InventoryUI))
    //        {
    //            UIManager.Instance.CloseUI(UIRootType.ContentUI, UIType.InventoryUI);
    //            UIManager.Instance.CloseUI(UIRootType.ContentUI, UIType.StorageUI);
    //            UIManager.Instance.CloseUI(UIRootType.PopupUI, UIType.FarmingUI);
    //        }
    //        else
    //        {
    //            UIManager.Instance.OpenUI(UIRootType.ContentUI, UIType.InventoryUI);
    //            UIManager.Instance.OpenUI(UIRootType.ContentUI, UIType.StorageUI);
    //            UIManager.Instance.OpenUI(UIRootType.ContentUI, UIType.FarmingUI);
    //        }
    //    }
    //}

    //// UI매니저 추가
    //public bool IsOpenUI(UIType uiType)
    //{
    //    return _opendUIList.Contains(uiType);
    //}

}
